streamlit>=1.37
langgraph>=0.2,<0.3
pydantic>=2.6

# Optional — only needed if you set LLM_PROVIDER=anthropic|openai (see
# services/llm_client.py). The app runs fully offline without these.
# anthropic>=0.34
# openai>=1.40
